# Normalize line endings: LF in the repository, native on checkout.
# Keeps checksums and diffs stable across Windows/Linux contributors.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.ico binary
*.otf binary
*.ttf binary
